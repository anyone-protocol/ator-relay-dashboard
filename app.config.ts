export default defineAppConfig({
  ui: {
    primary: "cyan",
    gray: "neutral",
    modal: {
      overlay: {
        background: "bg-gray-200/75 dark:bg-gray-800/75 backdrop-blur",
      },
    },
  },
});
